# Runtime state, exported reports, and any real scan output, never commit.
# Exports written here contain a real inventory of the local machine.
DATA/

# Packaged build artifact (regenerate with PyInstaller, see README)
*.exe

# App-icon raster (the .ico is enough for the build)
*.png

# Build / packaging
build/
dist/
*.spec
__pycache__/
*.pyc

# Local tooling and editor config
.claude/
*.code-workspace
.vscode/
.idea/

# Python environments
venv/
.venv/
env/

# OS noise
Thumbs.db
.DS_Store
